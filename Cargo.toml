[workspace]
members = [
    "beacon-core",
    "edr-evasion",
    "exfil-channels",
    "loader-stubs"
]

[workspace.package]
version = "0.1.0"
authors = ["NCDD Red Cell <ops@ncdd.no>"]
edition = "2021"

[workspace.dependencies]
# Shared dependencies for consistency
windows-sys = "0.48"
aes-gcm = "0.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
