# NSM Red Team Tool - Environment Variables
# Copy this file to .env and fill in your values
# DO NOT commit .env to git (it's in .gitignore)

# Encryption key for log files (use strong passphrase - min 16 characters)
REDTEAM_ENCRYPTION_KEY=your-strong-passphrase-here-change-this

# OpenAI API key for judge LLM (only if using judge_llm in config)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Kill switch file path (default: /tmp/nsm_redteam_kill)
# The tool checks for this file and aborts if it exists
REDTEAM_KILL_SWITCH=/tmp/nsm_redteam_kill

# Proxy settings (if not using browser.proxy in config)
# HTTP_PROXY=http://proxy:8080
# HTTPS_PROXY=http://proxy:8080